# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
.env

# Downloaded model weights (e.g. sam3.pt) — too large for GitHub LFS (2 GiB/file max); download separately
*.pt

# Exported segment cutouts (see SEGMENTS_OUTPUT_DIR)
segments/
